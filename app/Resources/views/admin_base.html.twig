{% extends "::layout/_base.html.twig" %}

{% block title %}Admin - {% endblock %}

{% block body %}
    <div id="admin" class="row fill">

        <nav class="col-sm-3 col-md-2 main-nav sidebar fill">
            <h1 class="permanent-marker text-center">
                The Usual Dreamers
            </h1>
            <aside>
                Some information
            </aside>
            <ul class="nav side-menu">
                <li>
                    <a href="#" title="{{ 'header.navigation.blog'|trans }}">
                        <i class="fa fa-keyboard-o" aria-hidden="true"></i>
                        <label>{{ 'header.navigation.blog'|trans }}</label>
                    </a>
                </li>
                <li>
                    <a href="#" title="{{ 'header.navigation.blog'|trans }}">
                        <i class="fa fa-binoculars" aria-hidden="true"></i>
                        <label>{{ 'header.navigation.equipments'|trans }}</label>
                    </a>
                </li>
                <li>
                    <a href="{{ path('blog_post_list') }}" title="{{ 'header.navigation.publicSite'|trans }}">
                        <i class="fa fa-globe" aria-hidden="true"></i>
                        <label>{{ 'header.navigation.publicSite'|trans }}</label>
                    </a>
                </li>
                <li>
                    <a href="{{ path('fos_user_security_logout') }}" title="{{ 'header.navigation.logout'|trans }}">
                        <i class="fa fa-power-off" aria-hidden="true"></i>
                        <label>{{ 'header.navigation.logout'|trans }}</label>
                    </a>
                </li>

            </ul>
        </nav>

        <section class="col-sm-9 col-md-10 main fill">
            <div>
                <ol class="breadcrumb">
                    {% block breadcrumbs %}
                        <li><a href="#">Admin</a></li>
                    {% endblock %}
                </ol>
            </div>
            <aside>
                {% for type, flash_messages in app.session.flashBag.all %}
                    {% for flash_message in flash_messages %}
                        <div class="alert alert-{{ type }}">
                            <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span
                                        aria-hidden="true">&times;</span></button>
                            {{ flash_message }}
                        </div>
                    {% endfor %}
                {% endfor %}

            </aside>

            <main>
                {% block content %}{% endblock %}
            </main>
        </section>
    </div>
{% endblock %}